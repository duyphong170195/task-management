<?xml version="1.0" encoding="UTF-8"?>
<configuration>

<appender name="CONSOLE"
          class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
<!--        <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} ${spring.application.name:-} [%thread] %-5level %logger{36} [traceId=%X{traceId}] - %msg%n</pattern>-->
        <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SXXX}|2|%level|${spring.application.name:-}|%X{X-B3-TraceId:-},%X{X-B3-SpanId:-},%X{X-Span-Export:-},%X{traceId}|[traceId=%X{traceId} spanId=%X{spanId}]|${PID}|%thread|%logger{40}|%message%n</pattern>
        <charset>utf8</charset>
    </encoder>
</appender>

<root level="INFO">
    <appender-ref ref="CONSOLE"/>
</root>

</configuration>
